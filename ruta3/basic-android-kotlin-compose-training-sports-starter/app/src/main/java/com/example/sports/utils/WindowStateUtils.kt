package com.example.sports.utils

// ---------- 6. Paquete utils + enum SportsContentType (ListOnly / ListAndDetail) ----------
/**
 * Content shown depending on size and state of device.
 */
enum class SportsContentType {
    ListOnly, ListAndDetail
}
